<view>
  <view wx:for="{{songList}}" wx:key="{{item.id}}" class="songs-box">
    <image class="img" src="{{item.al.picUrl}}"></image>
    <text>{{item.al.name}}</text>
    <navigator url="/pages/player/player?id={{item.id}}&name={{item.al.name}}&singer={{singer}}">播放</navigator>
  </view>
</view>
