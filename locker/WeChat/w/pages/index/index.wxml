<view>
  <view wx:for="{{list}}" wx:key="{{item.id}}" class="singer-box">
    <image class="img" src="{{item.picUrl}}"></image>
    <text>{{item.name}}</text>
    <navigator url="/pages/songs/songs?id={{item.id}}&singer={{item.name}}">查看单曲</navigator>
  </view>
</view>
